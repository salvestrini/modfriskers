autogen definitions sitetool;

common = {
	layout = "./layout.sxml";
};

vars = {
	var-sitename = "Grammar Extractors";
};

include "contents.as"
include "pages.as"
include "map.as"
include "files.as"
